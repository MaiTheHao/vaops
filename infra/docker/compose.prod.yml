name: vaops
services:
  postgres:
    image: postgres:17-alpine
    container_name: vaops-db
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
      - TZ=UTC
    networks:
      - vaops-network
    volumes:
      - vaops_pg_data:/var/lib/postgresql/data
      - ./scripts:/docker-entrypoint-initdb.d

    command: >
      postgres 
      -c 'shared_buffers=1GB' 
      -c 'work_mem=32MB'
      -c 'maintenance_work_mem=256MB'
      -c 'effective_cache_size=4GB'
      -c 'max_connections=100'

    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -d ${DB_NAME}']
      interval: 5s
      timeout: 5s
      retries: 5

  flyway:
    image: flyway/flyway:10
    container_name: vaops-flyway
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vaops-network
    volumes:
      - ../database/migrations:/flyway/sql
    environment:
      - FLYWAY_URL=jdbc:postgresql://postgres:5432/${DB_NAME}?currentSchema=public
      - FLYWAY_USER=${POSTGRES_USER}
      - FLYWAY_PASSWORD=${POSTGRES_PASSWORD}
      - FLYWAY_LOCATIONS=filesystem:/flyway/sql
      - FLYWAY_BASELINE_ON_MIGRATE=true
      - FLYWAY_CONNECT_RETRIES=10
    command: migrate
    restart: 'no'

  backend:
    image: ${DOCKER_USERNAME}/vaops-backend:latest
    container_name: vaops-backend
    restart: unless-stopped
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
      - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD}
    networks:
      - vaops-network
    depends_on:
      postgres:
        condition: service_healthy
      flyway:
        condition: service_completed_successfully

  frontend:
    image: ${DOCKER_USERNAME}/vaops-frontend:latest
    container_name: vaops-frontend
    restart: unless-stopped
    networks:
      - vaops-network
    depends_on:
      - backend

  nginx:
    image: nginx:1.27-alpine
    container_name: vaops-nginx
    restart: unless-stopped
    ports:
      - '80:80'
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - vaops-network
    depends_on:
      - frontend
      - backend

volumes:
  vaops_pg_data:
    name: vaops_database_volume

networks:
  vaops-network:
    driver: bridge
